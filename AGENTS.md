# boss-cli 项目操作约束

## BOSS 网页必须使用同一专用浏览器

- 用户要求打开或显示 BOSS 业务页面时，执行 `node bin/boss.js open`（安装后可用 `boss open`）。该命令核验专用 profile、冷却和登录态，优先展示已有页面，并释放自动化连接及命令锁，让用户继续操作。该入口不接受任意 URL 参数。
- 不得用 Codex 侧边浏览器、通用 `open_in_codex` browser 目标、系统默认浏览器、个人 Chrome profile 或其他临时 profile 打开 BOSS 页面。此类入口不共享本项目登录环境，也不经过本项目访问保护。
- `boss open` 因未登录、冷却、锁冲突、浏览器配置异常而失败时，按错误处理；不得改走另一浏览器作为兜底。
- 只展示刚才已有的网页时使用 `boss open --existing-only`，没有目标页面就停止，不额外访问首页。
- 对用户已经打开的受限页，可以只读检查原页面作为证据；不要刷新或再次导航，也不要用另一个浏览器的正常页面替代其检查结果。系统查看器打开本地 `qr.html` 或截图不属于远程 BOSS 导航。
- 自动化和端到端测试通过本项目 `openContext` 以及 `context.accessGuard`，同一任务复用 context，结束后 `close()`。只有用户要求把页面留给自己操作时才 `handoff()`；交接后的手工浏览不计入自动化预算，也不能宣称受程序保护。
- 不复制登录 Cookie 到其他浏览器，不手工删除有效命令锁或修改冷却记录以重试。对已证实的本地误判做状态修复时，必须先保留证据，限定到该条误判记录，并保留累计用量。

## 受限状态与结论边界

- 发现受限提示，先核对浏览器、profile、当前 URL、登录标志和提示时间。不能把一个浏览器的成功或失败扩大成其他浏览器、所有请求或账号全局状态。
- 明确区分本地策略暂停、平台访问限制、人工验证要求和登录失效。看见 `captcha` 字样的接口名称不能当作验证码页面；导航到安全检查页面也不等于已经确认封禁根因。
- 不刷新受限页、不换 IP/账号/profile 重试，不修改平台检测脚本或自动解决验证码。
- 没有服务端记录时，触发原因只能作为假设；一次通过不能证明长期防封效果。
- 真实发送遵守用户指定的对象、内容与次数；仅点击成功不算发送成功，结果未知时不能重发。

## 验证

- 改动浏览器生命周期、打开入口或访问保护时，优先使用独立临时 profile 的真实 Chrome 和 localhost 验证，禁止拿用户账号做循环或限流压力测试。
- 保留既有未提交改动，不将 Cookie、profile、二维码或含凭证的网络响应纳入版本控制。
